# Exercise 3 — Build your own image
# Concepts: FROM, COPY, RUN, CMD, docker build, layer cache
#
# Complete T1-T4, then from this folder run:
#   docker build -t my-first-image .
#   docker run --rm my-first-image
# Expected output:  Hello from an image I built myself!
# -----------------------------------------------------------------------

# T1 — Start FROM a small base image.
#       Form: FROM <image>:<tag>     Hint: ubuntu:24.04
#
# YOUR INSTRUCTION HERE


# T2 — COPY the greet.sh script from this folder into the image at /greet.sh.
#       Form: COPY <source-on-host> <destination-in-image>
#       Hint: COPY greet.sh /greet.sh
#
# YOUR INSTRUCTION HERE


# T3 — Make the script executable, as a build step.
#       Form: RUN <command>
#       Hint: RUN chmod +x /greet.sh
#
# YOUR INSTRUCTION HERE


# T4 — Set the default command to run the script when a container starts.
#       Form: CMD ["<executable>", "<arg>", ...]
#       Hint: CMD ["/greet.sh"]
#
# YOUR INSTRUCTION HERE
